package br.cesed.unifacisa.si.exemplos;

public enum EnumSingleClass {

	// Classe como um Enum de um valor só.
	INSTANCE;

    // Métodos aqui

	
	
	// Agora você pode acessar isso da seguinte forma:
	//EnumSingleClass.INSTANCE;
	
	/*
	 * Considerações:
	 * 
	 * Existem muitas formas diferentes de usar um padrão como o Singleton.
	 * Cada uma pode ser boa ou ruim para determinadas situações e algumas escondem
	 * certos problemas.
	 * 
	 * Entretanto, um vez que se compreenda um pouco a diferença entre as implementações,
	 * não é difícil escolher uma que se encaixe melhor na sua solução.
	 * 
	 */
}
